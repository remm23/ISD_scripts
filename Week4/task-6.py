# the int keyword throws an error
# in python their is no need to 
# define a type for the variable
int x = 2

# there is no double(") or single(')
# quotes  around "squared is"
print(x,squared is, x * x)

# this crashes because theirs one too many *
# only two is need to find the power of a number
xcubed = x *** 3
